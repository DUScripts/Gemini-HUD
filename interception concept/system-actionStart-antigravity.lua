if exportMode == false then
    exportMode = true
    system.print("The export mode is enabled ALT+G")
        else
        exportMode = false
    system.print("Export mode is disabled ALT+G")
    end